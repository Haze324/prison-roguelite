# Demo 稳定化与分阶段验收计划

## 当前状态

- 当前分支：`agent/phase-1-player-prototype`
- 最近已提交版本：`4555a65 fix: 修正撬棍朝向与背包拖拽`
- 该版本只作为可回退的功能备份，不视为稳定版本。
- 工作区内用户后来加入的素材、场景和资源不回滚、不覆盖、不自动混入修复提交。

## 工作规则

1. 暂停新增美术打磨和非必要功能，先恢复稳定闭环。
2. 一个阶段只处理一个模块；阶段未通过，不叠加下一模块。
3. 每个阶段必须同时通过：Godot 解析检查、headless 启动检查、实际运行操作检查、截图检查。
4. 自动测试代理负责监测 Godot 启动和错误输出；实际画面与输入路径由回归清单固定验证。
5. 发现问题时先记录复现步骤和归属模块，再修复；不在多个系统之间同时打补丁。

## 阶段与通过标准

### S0：基线审计

- 主场景可以加载，autoload、资源引用和输入映射无解析错误。
- 列出当前所有未提交文件，不回滚用户文件。
- 建立最小回归入口，明确每个后续阶段的操作步骤。

### S1：玩家基础闭环

- WASD 移动、碰撞阻挡、冲刺和状态机正常。
- 角色朝向跟随鼠标，武器朝向跟随鼠标。
- 右键瞄准时相机偏移、移动减速，松开恢复。
- 手电筒只有单一锥形光束，方向、开关和遮挡一致。

### S2：单一枪械战斗闭环

- 一把枪可以瞄准、射击、命中、造成伤害。
- 弹匣、备弹、换弹进度条和快捷栏图标状态一致。
- 受伤、血条、死亡和短暂视觉反馈正常。

### S3：近战与投掷物闭环

- 撬棍单独使用握持支点和攻击弧线，尺寸由武器资源可调。
- 普通投掷物左键蓄力、松开投掷，撞墙最多反弹两次，撞怪立即爆炸。
- 地雷必须长按完成安置进度，未完成不生成。

### S4：背包与装备

- 装备槽固定为：头、手、身体、武器1、武器2、回复血瓶、普通消耗品、投掷物1、投掷物2。
- Q 只使用回复血瓶；F 只使用弹药箱/肾上腺素等普通消耗品。
- 背包物品只显示图标，悬浮显示名称和数量。
- 拖放、交换、堆叠、槽位类型限制和快捷栏同步均可重复验证。
- 背包交互使用真实 Godot UI 节点和信号，避免继续扩大手写坐标命中测试的风险。

### S5：敌人、Boss 与地图交互

- 敌人状态机、受击、死亡、掉落和碰撞正常。
- Boss 出现只触发一次短暂警戒提示，不持续覆盖屏幕。
- 地图墙体、门、补给、商人、技能终端和撤离目标可交互。

### S6：可玩 Demo

- 从主菜单开始，完成一局最小流程：移动、战斗、补给、修复目标、Boss、撤离/失败。
- 连续运行三次无脚本错误、无资源为空、无输入卡死、无背包状态丢失。
- 每个阶段单独提交并推送，验收通过后才进入下一阶段。

## 当前明确的高风险项

- `scripts/systems/demo_hud.gd` 目前大量使用 `draw_*` 和手写坐标命中测试，背包拖放、悬浮提示和分辨率适配存在结构性风险。
- 无头测试只能证明解析和启动，不能代替真实输入、拖放、视觉层级和动画检查。
- 当前工作区有用户未提交文件，所有修复必须精确选文件提交。

