# 工业监狱墙体 TileSet 图例

`resources/maps/prison_tileset_v1.tres` 的源 0 只保留可识别的墙体结构，源 1 保留透明装饰。墙体单元统一为 `48×48`，墙带统一为 `16px`。

| 源 | 图集 | 尺寸 | 职责 |
|---|---|---:|---|
| 源 0：墙体 | `prison_wall_kit_48_v1.png` | 8×6，共 34 个可选块 | 地板、横墙、竖墙、4 转角、4 T 口、十字口、端头、门框；带碰撞与遮光 |
| 源 1：装饰 | `prison_modular_props_48_v1.png` | 9×9，共 81 个可选块 | 透明灯具和小型装饰覆盖；不携带墙面背景 |

## 源 0 坐标约定

| 坐标/范围 | 用途 |
|---|---|
| `(0…3, 0)` | 地板变体 |
| `(0…3, 1)` / `(4…7, 1)` | 上侧横墙 / 下侧横墙 |
| `(0…3, 2)` / `(4…7, 2)` | 左侧竖墙 / 右侧竖墙 |
| `(0…3, 3)` | 左上、右上、右下、左下转角 |
| `(0…3, 4)` | 四种 T 口；`(4,4)` 为十字口；`(5,4)` 为门框 |
| `(0…3, 5)` | 上、右、下、左四种端头 |

墙体连接掩码使用 `N=1, E=2, S=4, W=8`。每个块的透明区域与墙带位置严格对应，墙带宽度恒为 16px；转角和 T 口均由同一套横/竖墙带组合而成。
