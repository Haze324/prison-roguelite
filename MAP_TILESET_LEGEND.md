# 工业监狱 TileSet 图例

`resources/maps/prison_tileset_v1.tres` 使用一个真正可编辑的 48×48 图集源。

| 项目 | 规则 |
|---|---|
| 原始美术 | `prison_tileset_atlas_1024.png`，它是 7 列 × 8 行的素材联系表，而非规则 TileMap 图集 |
| 运行图集 | `prison_tileset_sliced_48_v6.png`；每一格对应原联系表中的一张完整素材 |
| 图块数量 | 56 个，全部为 1×1（48×48）且可选择 |
| 选择框 | 一个选择框对应一个完整素材，不存在把单个素材切为多个选择框的情况 |
| 使用层 | `FloorLayer`、`WallLayer`、`DoorFrameLayer`、`DecorationLayer` 都使用源 0 |

## 常用建筑坐标（源 0）

| 坐标 | 用途 |
|---:|---|
| `(0, 0)` | 裂纹地面 |
| `(1, 0)` | 金属地面 |
| `(2, 0)` | 横向墙体 |
| `(0, 1)` | 纵向墙体 |
| `(1, 1)` | 墙体转角 |
| `(2, 2)` | 门框 |
